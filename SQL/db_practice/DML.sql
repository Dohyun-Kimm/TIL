CREATE TABLE users(
    first_name TEXT NOT NULL,
    lasr_name TEXT NOT NULL,
    age INTEGER NOT NULL,
    coutnry TEXT NOT NULL<
    phone TEXT NOT NULL,
    balance INTEGER NOT NULL
);
-- 쿼리 구조
--  뭐 해라. 필드 이거 가지고, 어디에서? 표에서
